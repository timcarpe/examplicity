# Vendored lab-kit releases

Lab Creation owns the canonical `@examplicity/lab-kit` source and agent
distribution. The live site vendors immutable supported releases here so normal
publication builds never depend on a sibling checkout. Each version directory
records upstream and resource hashes; edit the canonical kit, release it, then
vendor and rebuild an explicitly reviewed lab batch.
