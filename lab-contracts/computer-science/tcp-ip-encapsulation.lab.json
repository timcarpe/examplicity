{
  "schemaVersion": 1,
  "relationship": "Wrapping application data in protocol layers connects TCP/IP encapsulation to a packet's journey and receiver unpacking.",
  "learnerLoop": { "action": "Select protocol wrappers, send the built packet and peel layers at the receiver.", "modelChange": "The packet gains or loses the selected protocol shells as it moves between hosts.", "evidence": "The packing mat, flight packet, receiver packet and status strip show each layer's role.", "nextDecision": "Add, remove or compare wrappers and explain what the receiver must unpack." },
  "invariants": ["The packet shells shown in flight and at the receiver match the wrappers selected.", "Receiver peeling removes the outer visible shell in order."],
  "safeAdaptations": ["Change application stories, endpoint labels or prompts while preserving layer construction and ordered unpacking."],
  "nonGoals": ["Sending a real TCP/IP packet or emulating complete network protocols."],
  "developerGuide": "https://www.examplicity.org/developer/lab-contract"
}
