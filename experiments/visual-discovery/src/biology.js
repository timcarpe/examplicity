const lesson=[
 {label:'Existing variation',title:'Change food availability without changing adult beaks.',intro:'Move the food toward deeper beaks. Compare their food advantage with the adult counts.'},
 {label:'Parents, then offspring',title:'Trace selected parents into the next generation.',intro:'Select 24 parents, then produce 72 offspring to replace the adults.'},
 {label:'One direction',title:'Test whether selection shifts the mean beak depth.',intro:'Keep food availability unchanged for at least three generations.'},
 {label:'Favour the middle',title:'Measure the effect of favouring middle-sized beaks.',intro:'Run at least three generations, then calculate the middle group’s frequency.'},
 {label:'Favour both extremes',title:'Test selection that favours both extremes.',intro:'Favour small and large beaks for at least four generations. Compare the middle group and both tails.'},
 {label:'Experiment',title:'Investigate how food availability changes trait frequencies.',intro:'Move or widen the food sources, then compare successive generations.'}
];
const N=72,COUNT=24,LO=4,HI=16;
const s={population:[],initial:[],parents:null,pending:null,generation:0,peaks:[{c:10,w:1.4}],history:[],baseline:null,changed:false};
let rng,generationTimer;
s.phase='idle';s.frequency='';s.frequencyChecked=false;s.lineage=[];
function random(seed){return function(){let t=seed+=0x6D2B79F5;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}
function normal(sd){let u=0,v=0;while(!u)u=rng();while(!v)v=rng();return sd*Math.sqrt(-2*Math.log(u))*Math.cos(2*Math.PI*v)}
function stats(pop){const mean=pop.reduce((a,v)=>a+v,0)/pop.length;return{mean,sd:Math.sqrt(pop.reduce((a,v)=>a+(v-mean)**2,0)/pop.length),middle:pop.filter(v=>v>=8.5&&v<=11.5).length/pop.length,left:pop.filter(v=>v<8.2).length/pop.length,right:pop.filter(v=>v>11.8).length/pop.length}}
function initial(){clearTimeout(generationTimer);s.phase='idle';s.frequency='';s.frequencyChecked=false;rng=random(9137);s.population=Array.from({length:N},()=>clamp(10+normal(1.85),4.08,15.92));s.initial=[...s.population];s.generation=0;s.parents=null;s.pending=null;s.baseline=stats(s.population);s.history=[{g:0,...s.baseline}];s.changed=false}
function reset(){s.peaks=[{c:10,w:1.4}];initial()}
function food(v){return s.peaks.reduce((a,p)=>a+Math.exp(-.5*((v-p.c)/p.w)**2),0)}
function fitness(v){let max=.001;for(let x=4;x<=16;x+=.05)max=Math.max(max,food(x));return clamp(food(v)/max,0,1)}
function selectParents(){rng=random(9137+step*100003+s.generation*7919+17);const pool=s.population.map((trait,id)=>({trait,id,w:.2+fitness(trait)**1.4})),parents=[];for(let i=0;i<COUNT;i++){let r=rng()*pool.reduce((a,p)=>a+p.w,0),j=0;for(;j<pool.length-1;j++){r-=pool[j].w;if(r<=0)break}parents.push(pool.splice(j,1)[0])}s.parents=parents;const total=parents.reduce((a,p)=>a+.25+fitness(p.trait)**1.1,0);s.pending=[];s.lineage=[];for(let i=0;i<N;i++){let r=rng()*total,p=parents[0];for(const candidate of parents){r-=.25+fitness(candidate.trait)**1.1;if(r<=0){p=candidate;break}}s.pending.push(clamp(p.trait+normal(.43),4.05,15.95));s.lineage.push(p.id)}}
function reproduce(){if(s.phase!=='idle')return;if(!s.pending)selectParents();s.phase='offspring';s.frequency='';s.frequencyChecked=false;controls();render();if(reduced.matches)commitGeneration();else generationTimer=setTimeout(commitGeneration,1500)}
function commitGeneration(){if(s.phase!=='offspring')return;s.population=s.pending;s.parents=null;s.pending=null;s.phase='idle';s.generation++;s.history.push({g:s.generation,...stats(s.population)});controls();render()}
function enter(){clearTimeout(generationTimer);s.phase='idle';if(step<=1){initial();s.peaks=[{c:step===0?10:13.2,w:1.4}]}if(step===2){if(s.generation!==1)initial();s.peaks=[{c:13.2,w:1.4}]}if(step===3){initial();s.peaks=[{c:10,w:.7}]}if(step===4){initial();s.peaks=[{c:6.8,w:.8},{c:13.2,w:.8}]}if(step===5){initial();s.peaks=[{c:13.2,w:1.4}]}controls()}
function controls(){const c=$('controls');c.innerHTML='';setupBiologyWork();if(s.phase!=='idle')return;if(step===1||step===5)button(s.parents?'Produce offspring':'Select parents',()=>{if(s.parents)reproduce();else{selectParents();controls();render()}});if(step>=2&&step<=4)button('Next generation',reproduce);if(step===5){button(s.peaks.length===1?'Use two food sources':'Use one food source',()=>{if(s.peaks.length===1)s.peaks=[{c:6.8,w:.9},{c:13.2,w:.9}];else s.peaks=[{c:10,w:1.4}];s.parents=null;s.pending=null;controls();render()});const label=document.createElement('label');label.textContent='Food range ';const range=document.createElement('input');range.type='range';range.id='foodWidth';range.min='.55';range.max='3';range.step='.05';range.value=s.peaks[0].w;range.setAttribute('aria-label','Range of beak depths favoured by food');range.oninput=e=>{s.peaks.forEach(p=>p.w=Number(e.target.value));s.parents=null;s.pending=null;render();const next=c.querySelector('button');if(next)next.textContent='Select parents'};label.appendChild(range);c.appendChild(label);button('Reset population',()=>{initial();controls();render()},{class:'quiet'})}}
start();
function setupBiologyWork(){
 if(s.phase==='offspring'){workSetup('Inheritance',workRow('selected parents','<span class="lab-math-surface">24</span>')+workRow('offspring replacing the adults','<span class="lab-math-surface">72</span>'),'Each offspring inherits a trait from a selected parent, with variation.');return}
 if(step===0||step===1||step===2)workSetup('Calculate a trait frequency',workRow('deep-beaked birds ÷ all birds × 100',workOutput('frequencyWork')),'Frequency is the percentage of the population in this group.');
 if(step===3){workSetup('Calculate the middle group’s frequency',workRow('middle-sized birds ÷ all birds × 100','<span id="middleCount"></span> ÷ 72 × 100 = <input id="frequencyInput" type="number" min="0" max="100" step="0.1" inputmode="decimal" aria-label="Percentage of birds in the middle group">%'),'Run at least three generations, then give the frequency to one decimal place.',true);$('frequencyInput').value=s.frequency;$('frequencyInput').oninput=e=>{s.frequency=e.target.value;s.frequencyChecked=false;render()};workAction('Check frequency',()=>{s.frequencyChecked=true;render()})}
 if(step===4)workSetup('Compare the middle group’s frequency',workRow('starting population',workOutput('initialFrequency'))+workRow('current population',workOutput('currentFrequency')),'Compare these frequencies with the groups at both ends of the distribution.');
 if(step===5)$('working').hidden=true;
}
function updateBiologyWork(){
 const m=stats(s.population),deep=s.population.filter(v=>v>11.5).length,middle=s.population.filter(v=>v>=8.5&&v<=11.5).length;
 if(step<=2)workValue('frequencyWork',`${deep} ÷ 72 × 100 = ${fmt(deep/72*100)}%`);
 if(step===3){workValue('middleCount',middle);const correct=s.frequency!==''&&Math.abs(Number(s.frequency)-m.middle*100)<=.11,observed=s.generation>=3&&m.sd<s.baseline.sd*.78,ok=s.frequencyChecked&&correct&&observed;$('frequencyInput').disabled=s.generation<3;$('workActions').querySelector('button').disabled=s.generation<3;$('frequencyInput').className=s.frequencyChecked?(correct?'good':'bad'):'';workState(s.frequencyChecked?(correct?'good':'bad'):'needed');if(observed)feedback(ok?'Correct. Middle-sized beaks are more frequent and the spread is narrower.':s.frequencyChecked?'Divide the middle group count by 72 and multiply by 100.':'Calculate the current middle group’s frequency to complete the investigation.',ok,s.frequencyChecked&&!correct)}
 if(step===4){const initial=s.initial.filter(v=>v>=8.5&&v<=11.5).length;workValue('initialFrequency',`${initial} ÷ 72 × 100 = ${fmt(initial/72*100)}%`);workValue('currentFrequency',`${middle} ÷ 72 × 100 = ${fmt(middle/72*100)}%`)}
}
