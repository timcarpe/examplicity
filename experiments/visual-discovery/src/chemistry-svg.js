// Apparatus geometry follows the existing gas lab: thermostatted bath, sealed piston,
// connected pressure gauge and a pressure–volume graph, disclosed after observation.
function sceneHeight(w){return step===2||step===4?(w<600?610:520):(w<600?390:350)}
function gasLayout(){const small=W<600,x=small?25:W*.07,y=small?157:91,width=small?W-83:W*.53,height=small?134:155;return{x,y,width,height,gx:small?W*.79:W*.84,gy:small?57:158,gr:small?41:84}}
function gauge(cx,cy,r,p){const pos=(value,rr=r)=>{const a=(200+(value-75)/225*140)*Math.PI/180;return[cx+rr*Math.cos(a),cy+rr*Math.sin(a)]};
 dot(cx,cy,r+5,{fill:'var(--lab-concept-slate-line)',stroke:'var(--lab-concept-slate)','stroke-width':1});dot(cx,cy,r,{fill:'white'});dot(cx,cy,r-7,{fill:'var(--lab-paper)',stroke:'var(--lab-line-subtle)','stroke-width':1});
 for(let v=75;v<=300;v+=25){line(...pos(v,r-11),...pos(v,r-20),{stroke:'var(--lab-concept-slate)','stroke-width':1.4});if(!mobile&&[75,150,225,300].includes(v))text(...pos(v,r-33),String(v),'small','middle')}
 line(cx,cy,...pos(p,r-22),{stroke:'var(--lab-concept-amber)','stroke-width':3});dot(cx,cy,5,{fill:'var(--lab-concept-slate)',stroke:'white','stroke-width':1.5});text(cx,cy+(mobile?23:37),`${fmt(p)} kPa`,mobile?'accent':'big accent','middle');if(!mobile)text(cx,cy+60,'Pressure','small','middle');
 if(step===1){const pp=pos(s.prediction,r+1);line(cx,cy,...pp,{stroke:'var(--lab-concept-violet)','stroke-width':1.5,'stroke-dasharray':'4 4'});handle('pressurePrediction',...pp,{label:'Predicted pressure after expansion',min:75,max:300,value:s.prediction,valueText:`${fmt(s.prediction)} kilopascals`,increment:5,set:v=>{s.prediction=Math.round(v/5)*5;s.tested=false;s.V=3;render()},fromPoint:pt=>{let d=Math.atan2(pt.y-cy,pt.x-cx)*180/Math.PI;if(d<0)d+=360;return 75+(d-200)/140*225}});text(cx,cy+r+(mobile?24:27),`Prediction: ${fmt(s.prediction)} kPa`,'small','middle')}
}
function pressureGraph(){const left=mobile?48:W*.15,right=mobile?W-20:W*.84,top=mobile?402:334,bottom=mobile?560:476;const gx=v=>left+(v-2)/6*(right-left),gy=p=>bottom-(p-75)/225*(bottom-top);
 const plot=el('g',{'data-evidence':'pressure-volume-graph',role:'img','aria-label':`Pressure against volume. Current volume ${fmt(s.V,2)} litres, pressure ${fmt(600/s.V)} kilopascals.`});
 for(const p of [75,150,225,300]){line(left,gy(p),right,gy(p),{stroke:'var(--lab-line-subtle)','stroke-width':1},plot);text(left-10,gy(p)+4,String(p),'small','end',plot)}
 for(const v of [2,4,6,8]){line(gx(v),top,gx(v),bottom,{stroke:'var(--lab-line-subtle)','stroke-width':1},plot);text(gx(v),bottom+21,String(v),'small','middle',plot)}
 line(left,top,left,bottom,{stroke:'var(--lab-concept-slate)','stroke-width':1.3},plot);line(left,bottom,right,bottom,{stroke:'var(--lab-concept-slate)','stroke-width':1.3},plot);text(left,top-17,'Pressure / kPa','small','start',plot);text((left+right)/2,bottom+42,'Volume / L','small','middle',plot);
 // The curve is revealed by collected evidence, rather than supplied at first sight.
 if(s.records.length>=3||step===4){const points=[];for(let v=2;v<=8.001;v+=.04)points.push([gx(v),gy(600/v)]);el('path',{d:points.map((p,i)=>(i?'L':'M')+p.join(' ')).join(' '),fill:'none',stroke:'var(--lab-accent)','stroke-width':2},undefined,plot)}
 for(const [i,r]of s.records.entries()){dot(gx(r[0]),gy(r[1]),5,{fill:'var(--lab-concept-violet)',stroke:'white','stroke-width':1.5},plot);text(gx(r[0])+9,gy(r[1])-8,String(i+1),'small','start',plot)}
 const px=gx(s.V),py=gy(600/s.V);line(px,bottom,px,py,{stroke:'var(--lab-concept-amber-line)','stroke-dasharray':'4 4','stroke-width':1},plot);dot(px,py,6,{fill:'var(--lab-concept-amber)',stroke:'white','stroke-width':2},plot);
 if(!mobile)text(right,top-17,`(${fmt(s.V,2)} L, ${fmt(600/s.V)} kPa)`,'small','end',plot);
}
function render(){clear();head(lesson[step].title,lesson[step].intro);const {x,y,width,height,gx,gy,gr}=gasLayout(),displayedV=step===3&&!s.tested?4:s.V,px=x+width*displayedV/8,mid=y+height/2,bathX=mobile?8:x-30,bathY=mobile?116:41,bathWidth=mobile?W-16:width+115,bathHeight=mobile?210:237;
 chamber={x:x+7,y:y+7,width:Math.max(14,px-x-17),height:height-14};
 const defs=el('defs'),metal=el('linearGradient',{id:'metal',x2:'0%',y2:'100%'},undefined,defs);for(const [offset,color]of [[0,'#eef2f4'],[.45,'#c4cdd3'],[1,'#8d9ba6']])el('stop',{offset,'stop-color':color},undefined,metal);
 const clip=el('clipPath',{id:'gasClip'},undefined,defs);el('rect',{x:chamber.x,y:chamber.y,width:chamber.width,height:chamber.height,rx:3},undefined,clip);
 el('rect',{x:bathX,y:bathY,width:bathWidth,height:bathHeight,rx:17,fill:'var(--lab-concept-teal-soft)',stroke:'var(--lab-concept-teal-line)','stroke-width':1.5});
 let wave=`M ${bathX+10} ${bathY+28}`;for(let j=0;j<Math.floor((bathWidth-20)/36);j++)wave+=` q 9 -5 18 0 t 18 0`;el('path',{d:wave,fill:'none',stroke:'var(--lab-concept-teal-line)','stroke-width':1});text(bathX+13,bathY+19,'300 K · temperature held fixed','small');
 if(!mobile){const pipe=`M ${x-8} ${y+height*.55} V ${y+height+28} H ${gx-gr-42} Q ${gx-gr-10} ${y+height+28} ${gx-gr-3} ${gy+16}`;el('path',{d:pipe,fill:'none',stroke:'var(--lab-concept-slate)','stroke-width':6,'stroke-linejoin':'round'});el('path',{d:pipe,fill:'none',stroke:'var(--lab-concept-slate-line)','stroke-width':2,'stroke-linejoin':'round'})}
 el('rect',{x:x-12,y:y-8,width:width+66,height:height+16,rx:12,fill:'url(#metal)',stroke:'var(--lab-concept-slate)','stroke-width':1.5});
 el('rect',{x:x+1,y:y+1,width:Math.max(1,px-x-1),height:height-2,rx:4,fill:'var(--lab-concept-blue-soft)',stroke:'var(--lab-concept-blue-line)','stroke-width':1});line(x+7,y+5,px-7,y+5,{stroke:'white','stroke-width':3});el('rect',{x:x-5,y:y-1,width:12,height:height+2,rx:3,fill:'var(--lab-concept-slate)'});
 if(step===0){const target=x+width*3/8;line(target,y-5,target,y+height+8,{class:'ghost'});text(target,y+height+49,'3 L target','small','middle')}
 if(step===3&&!s.tested){const fp=x+width*s.forecast/8;el('rect',{x:fp-6,y:y-3,width:12,height:height+6,rx:3,class:'ghost'});text(fp,y-18,`${fmt(s.forecast,2)} L predicted`,'small','middle')}
 particleNodes=[];impactNodes=[];const group=el('g',{'clip-path':'url(#gasClip)'});for(const [i,p]of particles.entries()){if(!Number.isFinite(p.x)){p.x=5+(chamber.width-10)*((i*.6180339+.17)%1);p.y=5+(chamber.height-10)*((i*.4142136+.23)%1)}p.x=clamp(p.x,5,chamber.width-5);p.y=clamp(p.y,5,chamber.height-5);particleNodes.push(dot(chamber.x+p.x,chamber.y+p.y,mobile?3.8:4.4,{fill:'var(--lab-concept-blue)',stroke:'white','stroke-width':1.2},group));impactNodes.push(line(px-5,chamber.y+p.y-6,px-5,chamber.y+p.y+6,{stroke:'var(--lab-concept-amber)','stroke-width':3,opacity:0}))}
 el('rect',{x:px,y:mid-9,width:width+x+46-px,height:18,rx:5,fill:'url(#metal)',stroke:'var(--lab-concept-slate)','stroke-width':1});el('rect',{x:px-7,y:y-3,width:17,height:height+6,rx:4,fill:'var(--lab-concept-slate)',stroke:'var(--lab-concept-slate-ink)','stroke-width':1.5});line(px-3,y+8,px-3,y+height-8,{stroke:'var(--lab-concept-slate-line)','stroke-width':2});
 if(step!==1){const hx=step===3&&!s.tested?x+width*s.forecast/8:px;const h=handle('volume',hx,mid,{label:step===3?'Predicted gas volume':'Gas volume',min:2,max:8,value:step===3?s.forecast:s.V,valueText:`${fmt(step===3?s.forecast:s.V,2)} litres`,increment:.25,set:setVolume,fromPoint:p=>(p.x-x)/width*8});h.replaceChildren();h.classList.add('lab-boundary-grip');el('rect',{x:hx-21,y:y-5,width:46,height:height+10,class:'lab-boundary-hit'},undefined,h);el('rect',{x:hx+12,y:mid-22,width:48,height:44,rx:10,class:'lab-boundary-knob'},undefined,h);el('path',{d:`M ${hx+22} ${mid} H ${hx+50} M ${hx+29} ${mid-6} L ${hx+22} ${mid} L ${hx+29} ${mid+6} M ${hx+43} ${mid-6} L ${hx+50} ${mid} L ${hx+43} ${mid+6}`,class:'lab-boundary-arrow'},undefined,h)}
 const scaleY=y+height+25;line(x+width/4,scaleY,x+width,scaleY,{stroke:'var(--lab-concept-slate-line)','stroke-width':1.3});for(let v=2;v<=8;v++){const xx=x+width*v/8;line(xx,scaleY-4,xx,scaleY+4,{stroke:'var(--lab-concept-slate)','stroke-width':1});if(v%2===0)text(xx,scaleY+20,String(v),'small','middle')}text(x+width/2,scaleY+39,'Volume / L','small','middle');
 text(x+14,y+height-14,`${fmt(displayedV,2)} L`,'accent');gauge(gx,gy,gr,600/s.V);
 if(step===0)feedback(Math.abs(s.V-3)<.06?'Half the volume; twice the pressure. More piston impacts, with the same particle speed.':'The bath holds temperature steady while you compress the sealed gas.',Math.abs(s.V-3)<.06);
 if(step===1){const ok=s.tested&&Math.abs(s.prediction-100)<=5;feedback(ok?'Volume doubled from 3 L to 6 L. Pressure halved from 200 to 100 kPa.':s.tested?`Predicted ${fmt(s.prediction)} kPa; observed 100 kPa. Revise the marker and test again.`:'Place the violet marker on the gauge, then test the expansion.',ok,s.tested&&!ok)}
 if(step===2)feedback(s.records.length>=3?'The curve is inverse: each reading gives the same 600 kPa·L product.':`${s.records.length} of three different readings kept. The plotted points follow the piston.`,s.records.length>=3);
 if(step===3){const ok=s.tested&&Math.abs(Number(s.product)-600)<.01&&Math.abs(s.forecast-2.5)<.03;feedback(ok?'150 × 4 = 600. Then V = 600 ÷ 240 = 2.5 L. The gauge confirms the prediction.':s.tested?'Calculate the initial pV product, then divide it by the target pressure.':'The outlined piston is your prediction. The gas remains at the starting state.',ok,s.tested&&!ok);for(const id of ['productInput','volumeInput'])if($(id)){$(id).classList.toggle('good',ok);$(id).classList.toggle('bad',s.tested&&!ok)}}
 if(step===4)feedback(s.target?Math.abs(600/s.V-s.target)<1?'Target reached. Keep this reading, or make another prediction.':`Target pressure: ${s.target} kPa.`:`pV = ${fmt(600/s.V)} × ${fmt(s.V,2)} = 600 kPa·L.`,false);
 if(step===2||step===4)pressureGraph();if(step>=2)table(['Reading','V / L','p / kPa','pV / kPa·L'],s.records.map((r,i)=>[i+1,fmt(r[0],2),fmt(r[1],2),'600']));endDraw();
}
